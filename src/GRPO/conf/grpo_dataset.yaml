# 供 dataset_gen_toxUnikp.py（Hydra 版）使用
iteration_num:  ${oc.env:ITER,0}
label: ${oc.env:LABEL,3.1.1.1}
run_dir: ${oc.env:RUN_DIR}

model_dir: "/home/bingxing2/ailab/group/ai4earth/hantao/project/internTA/proteinGflownet/ZymCTRL_local"

paths:
  output_logs:  "${run_dir}/logs.csv"
  sequences:    "${run_dir}"
  tm_scores:    "${run_dir}"
  toxicity:     "${run_dir}/outfile${iter}.csv"        # 也可以在 .sh 里覆盖
  kcat:         "${run_dir}/results_kcat${iter}.csv"   # 同上

hydra:
  job:
    chdir: false
